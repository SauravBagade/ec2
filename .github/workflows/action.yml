name: Deploy EasyCRUD to EC2

on:
push:
branches:
- main

jobs:
deploy:
runs-on: ubuntu-latest

```
steps:
  - name: Checkout repository
    uses: actions/checkout@v4

  - name: Test workflow
    run: echo "Workflow syntax is correct"
```
